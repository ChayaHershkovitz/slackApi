name: Update Slack on catalog.yaml changes

env:
  TOKEN: xoxp-5492487209540-5475511713527-5602828147394-1b9bde36e8aad0070a9424f1600fdadd

on:
  push:
    branches:
      - main

jobs:
  update-slack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Build and run app.js
        run: |
          npm i
          node api
